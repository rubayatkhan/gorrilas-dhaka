# Next Session Brief

Use this as the first message in a new session:

"Build a Phaser + TypeScript game prototype in this folder for a modern Dhaka-themed remake of QBasic GORILLAS. Start by scaffolding the project and implementing a playable MVP: two-player hot-seat, angle/power/wind projectile physics, destructible buildings, collision/explosions, round reset, and minimal HUD. Keep code modular for future power-ups and online mode. Then set up image generation prompts and pipeline for Dhaka-themed assets (skyline layers, characters, UI panels, FX sprites)."

## Immediate Next Commands (suggested)
1. Initialize project (`npm create vite@latest` + Phaser setup OR chosen starter).
2. Add game scene architecture (`BootScene`, `GameScene`, `UIScene`).
3. Build physics and turn system with placeholder rectangles/sprites.
4. Generate/plug real art assets.
